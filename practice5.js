function averageResponseTime(times) {
  if (Array.isArray(times) === false) {
        return "Invalid";
   }
   
   if (times.length ===0) {
        return "Invalid";
    }
   
 let total = 0;
    for (let i = 0; i < times.length; i++) {

        console.log(i)
        // if(typeof i===){


        // }
        // total = total + time[i];
    }
   
//   return total / times;
}
